# Read-tool filter semantics — behavioral specification

**OMN-148, section 1.** Status: **DRAFT — pending review.** Baseline: the OMN-151/156 PR tree (post-OMN-151/156/157;
previously `main` @ `5b41534`, advanced from `d89d5b7` at the 2026-06-11 review gate). Statements describe intended
behavior; where the implementation diverges, the Drift register (§8) records it. Provenance uses stable anchors (paths,
ticket IDs, commits, vault note titles) — never line numbers.

## 1. Scope and method

Covers filter semantics of `omnifocus_read` for tasks, projects, folders, and tags queries: filter vocabulary, modes,
logical composition, and result shaping as it affects what a filter returns. Excludes: response envelope details,
export, analytics (later OMN-148 sections).

Reconstructed from a five-source parallel audit (2026-06-11):

| Source                                                                                                        | What it contributed                          |
| ------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| Implementation (`QueryCompiler.ts`, `src/contracts/ast/` read side, `src/tools/tasks/task-query-pipeline.ts`) | Actual semantics                             |
| Advertised contract (`inputSchema`, tool description, SKILL.md, docs/dev)                                     | Promises                                     |
| Linear OMN history + CHANGELOG + git log                                                                      | Decisions, bug classes, why-it-is-this-way   |
| Test suite (unit + integration)                                                                               | Pinned behavior, defect-locks, coverage gaps |
| Obsidian vault (diary, audits, GTD working docs)                                                              | Requirements layer (§7)                      |

## 2. Design principles (normative)

| #   | Principle                                                                                                                                                                                                                                                   | Origin                                                                                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| P1  | **Strict boundary.** Every accepted filter key has defined semantics; unknown keys error. Accept-then-ignore is forbidden anywhere in the compile path.                                                                                                     | commit `0ff71a3`; OMN-76/90; Description-Gap Audit 2026-05-18 ("silent field-dropping is the worst class") |
| P2  | **Conjunctive composition.** All supplied filters AND-compose. A filter is never silently dropped or overridden by a sibling.                                                                                                                               | OMN-142 (`f.text ?? f.search` dropped `name` when both sent)                                               |
| P3  | **No silent widening.** A filter the compiler cannot express MUST produce a validation error — never compile to match-all.                                                                                                                                  | OMN-131 (violation closed 2026-06-11, `5b41534` — §8 D1)                                                   |
| P4  | **Honest results.** Truncation is unmistakable (`metadata.total_count` + limit visible); a cache may never serve a response compiled from a different predicate.                                                                                            | 25-row default misleading audits (§8 D13); 2026-05-19 incident, re-diagnosed as predicate drop (§8 D10)    |
| P5  | **Reads gate writes.** Selection bugs are data-loss bugs: sweeps delete what queries select. P2/P3 are safety properties, not ergonomics.                                                                                                                   | OMN-142 collateral deletion 2026-06-09                                                                     |
| P6  | **Normalize-then-strict front door.** The server accepts more than `inputSchema` advertises: strict parse first; bounded repair (wrapper-lift, stringified-JSON coercion) only on ZodError; strict re-validation; canonical inputs untouched byte-for-byte. | OMN-122; commit `06ba568`                                                                                  |
| P7  | **Field symmetry.** Any field that is writable and returned on read is also filterable.                                                                                                                                                                     | OMN-114 (`parentTaskId` precedent)                                                                         |
| P8  | **Schema-accepted ⇒ runtime-effective.** Parity tests guard against accepted-but-inert keys.                                                                                                                                                                | OMN-115 (`fastSearch` was a documented no-op), OMN-50 (`status:dropped` no-op)                             |

## 3. Filter vocabulary — tasks queries

### 3.1 Status and availability

| Filter                           | Semantics                                                                                                                                                                                                                                                              | Provenance                                    |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `status` enum                    | `active` \| `completed` \| `dropped` (tasks); `on_hold` affects projects only — no task-level meaning. `status: "on_hold"` on a tasks/export query rejects with steering (OMN-166).                                                                                    | `QueryCompiler.transformStatus`; OMN-50       |
| `completed` boolean              | Direct alias for the completion dimension; **overrides** `status`-derived completion when both supplied (documented exception to P2's no-override rule)                                                                                                                | OMN-72, commit `8b8d4e6`                      |
| `status: 'dropped'`              | The WIRE path for dropped-state queries; compiles onto the internal `TaskFilter.dropped` boolean. A bare `dropped` filter key is REJECTED by the strict schema (internal vocabulary, like `onHold`/`done` — live-proven 2026-06-11). Was a silent no-op before OMN-50. | commits `2673f34`, `13ad6c9`; OMN-157 test    |
| `available` / `blocked` booleans | OmniFocus availability model via `Task.Status` enum comparison (defer elapsed, not sequentially blocked, project active) — NOT mere incompleteness                                                                                                                     | `src/contracts/ast/types.ts` synthetic fields |
| `flagged` boolean                | Direct flag state                                                                                                                                                                                                                                                      | passthrough                                   |
| `inInbox` boolean                | Inbox membership = `task.inInbox` — **never** `!containingProject`                                                                                                                                                                                                     | commit `098c5cb`                              |

**Three terminal states rule:** completed, dropped, and active are distinct. Normative: default-safe queries and ALL
modes exclude `completed` AND `dropped`. Implemented for the `MODE_DEFINITIONS` modes (commit `e703ec7`, after dropped
tasks leaked into a weekly review — vault `gtd-review-handoff.md`). The bare/search/all/inbox gap (D14) was closed by
the OMN-157 fix (PR #90): the default dropped-exclusion routes through the AST emitter — `task.dropped` is synthetic in
OmniJS, so a script-level property check would be (and briefly was) a silent no-op.

### 3.2 Text

| Filter             | Semantics                                                                                                                                                                                                        | Provenance                                     |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `name: {contains}` | Case-insensitive substring on the **name field only**. Never matches notes.                                                                                                                                      | OMN-142, PR #84 `d89d5b7`                      |
| `name: {matches}`  | Case-insensitive regex on the name field only. Both sides escape the pattern via the `RegExp` constructor — the task-side raw-interpolation hazard was fixed by OMN-149 (`144d7b5`, 2026-06-11; §8 D6 resolved). | OMN-142 / OMN-149                              |
| `text: {contains}` | Substring on name OR note                                                                                                                                                                                        | commit `dfee69a` (deliberate name+note design) |
| `text: {matches}`  | Case-insensitive regex on name OR note                                                                                                                                                                           | `emitters/omnijs.ts`                           |
| `search`           | Legacy alias of `text` — permanent public/internal boundary, not removable debt                                                                                                                                  | OMN-25 ruling                                  |
| `fastSearch: true` | Restricts `text`/`search` matching to name only (skips note bodies). Was a documented no-op until 2026-05-31.                                                                                                    | OMN-115, PR #60                                |

`name` and `text` supplied together AND-compose as independent conditions (P2; pre-OMN-142 the name filter was silently
dropped). Text search is an inherent full `flattenedTasks` scan — no indexed primitive exists (OMN-115 analysis); see §5
performance classes.

**History (the OMN-142 drift, fixed 2026-06-11):** before `d89d5b7`, `name` compiled onto the `search` alias — matching
note content (collateral deletion of a real task, 2026-06-09) and silently degrading `matches` to substring. Two unit
tests pinned the defect ("transforms name.contains to search"). Deployments older than `d89d5b7` still carry it.

### 3.3 Dates

| Filter                                                  | Operators                      | Provenance                                       |
| ------------------------------------------------------- | ------------------------------ | ------------------------------------------------ |
| `dueDate`, `deferDate`, `plannedDate`, `completionDate` | `before` / `after` / `between` | DATE_FILTER_DEFS, `QueryCompiler.transformDates` |
| `added` (creation timestamp)                            | `before` / `after` / `between` | OMN-48, commit `daf32c4`                         |

- `before` = inclusive ≤, `after` = inclusive ≥; `between` = inclusive both ends (integration-verified).
- Every date condition wraps in an existence guard: tasks with no value for the field never match (OMN-53 — null ≠
  epoch-zero).
- `plannedDate` (OF 4.7+) is a **first-class peer** of dueDate/deferDate with the identical operator surface —
  load-bearing for the planned-date GTD workflow (§7 R5).
- Input format: `YYYY-MM-DD` or `YYYY-MM-DD HH:mm`, never ISO-8601 with `Z` (CLAUDE.md).
- Known inconsistency: the `deferDate` registry entry omits `operatorKey`, so `between` never sets `deferDateOperator`
  (§8 D4).

### 3.4 Containment

| Filter                             | Semantics                                                                                                                                                                                              | Provenance                                               |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- |
| `project: "<string>"`              | Name-or-id with name-lookup fallback; ambiguous names surface ALL candidates in a warning with `duplicateProjects` metadata — never silently pick one                                                  | OMN-34, v4.1.0                                           |
| `project: null`                    | Inbox (`inInbox: true`)                                                                                                                                                                                | `QueryCompiler`                                          |
| `projectId`                        | O(1) fast path via `Project.byIdentifier`; takes precedence over `project`                                                                                                                             | OMN-43                                                   |
| `parentTaskId: "<id>"`             | Direct children of a task. String only — `null` ("top-level") deliberately deferred, unlike `folder: null`                                                                                             | OMN-114                                                  |
| `folder` (tasks)                   | Rejects with steering on tasks/export queries (OMN-162) — error names the projects-by-folder → tasks-by-projectId recipe; capability work parked as OMN-167. Fully supported on projects queries (§5). | `QueryCompiler` via TASK_KEY_DISPOSITION                 |
| `tags: {any}` / `{all}` / `{none}` | OR / AND / NOT-IN over tag names. `{none}` is the supported exclusion path (NOT composition is not — §3.6). Must tolerate case-duplicate and same-name-different-id tags.                              | `transformTags`; vault Tag Inventory Redesign 2026-06-03 |

**Match-all guard (OMN-162):** a filter that compiles to zero conditions (e.g. `tags:{any:[]}` as the sole filter)
rejects with a steering error — it would otherwise match every task.

**Project root node (Q1 resolved 2026-06-11):** in OmniFocus a project IS a task (its root); the root appears in
`flattenedTasks` and reports its own project as `containingProject`. **Normative:** tasks queries exclude project root
rows by default; `includeProjectRoot: true` opts back in (Forecast parity — the OF UI shows due projects as items,
OMN-133); any returned root row carries `isProjectRoot: true`. Rationale: the root row leaks the project-is-a-task
implementation detail into the GTD domain model — counts diverge from the UI by +1 per project (vault diary 2026-05-19),
and an unmarked root feeding a write sweep is a P5 hazard (completing/deleting a root completes/deletes the project;
live-confirmed indistinguishable, 2026-06-11). Current behavior drifts (§8 D12, OMN-153).

### 3.5 Numeric

`estimatedMinutes: {equals | lessThan | greaterThan | between}` — range filters exclude tasks with no estimate
(existence-guarded; null ≠ 0). OMN-49/OMN-53.

### 3.6 Logical composition

| Form         | Semantics                                                                                                                                                                                                                                                                                                                 | Provenance                        |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| (implicit)   | All top-level filters AND-compose (P2)                                                                                                                                                                                                                                                                                    | —                                 |
| (siblings)   | Base fields beside AND/OR/NOT AND-compose (OMN-151; previously silently dropped — V1). Multiple operators together all apply (V4).                                                                                                                                                                                        | `transformFilters` + OMN-151      |
| `OR: [...]`  | Top-level OR groups, each branch independently compiled → `orBranches` → OmniJS `\|\|`. Base fields and other operators AND-compose with the OR group (OMN-151; previously siblings were silently dropped). `OR: []` and empty branches reject. Supported since v4.0.0. Docs claiming "OR is broken" are stale (OMN-104). | OMN-30, commit `d32c26b`; OMN-151 |
| `AND: [...]` | Explicit AND; merges one level deep with conflict detection — a key acquiring two different values rejects (was last-wins). Empty arrays and empty items reject.                                                                                                                                                          | `transformFilters` + OMN-151      |
| `NOT: {...}` | **Supported payloads only:** `{status:'completed'}`, `{status:'active'}`. Everything else hard-rejects with a validation error (OMN-131, shipped `5b41534` 2026-06-11; §8 D1 resolved — previously compiled silently to match-ALL). Tag exclusion belongs to `tags:{none}`. Seam: `transformNot` (QueryCompiler).         | OMN-131                           |

V-numbers (V1–V7) cite the verified-premise catalog in the OMN-151/156 design doc
(`docs/superpowers/specs/2026-06-12-omn-151-156-logical-operator-honesty-design.md` §1).

## 4. Modes — tasks queries

Modes defined in `MODE_DEFINITIONS` (today/upcoming/overdue/flagged/available/blocked/smart_suggest) add
`completed: false AND dropped: false` implicitly (three-terminal-states rule). `search`/`all`/`inbox` and bare filter
queries get the same two exclusions via the AST-compiled default (D14 resolved — OMN-157, PR #90). Modes compose with OR
filters since OMN-151 — previously the mode's augmentation keys were silently discarded beside `orBranches` (V6).

| Mode                    | Predicate                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `today`                 | due within `daysAhead` (default 3) OR flagged     | `reason` field categorizes overdue / due_soon / flagged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `upcoming`              | due within `daysAhead` (default 7)                | **Normative (Q2 resolved 2026-06-11):** default is 7; the tool description's "default 14" is a self-contradiction (its own example says 7), corrected via OMN-154. `daysAhead` defaults differ by mode (today=3, upcoming=7) and accept 1–30. `upcoming` is an MCP construct — OmniFocus has no such mode; the app's analog is the user-configurable Due Soon setting, reachable as `Task.Status.DueSoon`. Deriving due-soon windows from the app setting is noted as non-normative future work (OMN-130 territory).                                                                          |
| `overdue`               | `dueDate < now` — **dueDate only**                | NOT OmniFocus Forecast "Past" parity: Forecast Past = `dueDate < startOfToday OR plannedDate < startOfToday AND NOT blocked`. Parity recipe: union `mode:overdue` with `plannedDate:{before:now}`, merge by ID. OMN-133 SHIPPED `mode:forecast_past`, which runs exactly this union (dueDate-overdue + plannedDate-past, excluding blocked) and merges by id — note it is intentionally broader than the OF UI "Past" section, where dueDate-overdue items bucket under "Today". Residual ±2 candidates: overdue inbox tasks, project-root-as-task, on-hold parents (vault diary 2026-05-20). |
| `flagged`               | flagged                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `available` / `blocked` | availability model (§3.1)                         |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `search`                | no augmentation — filters pass through unmodified | no mode augmentation; terminal-state exclusion applies via the script-level completed default + the AST-compiled dropped default (D14 resolved, OMN-157)                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## 5. Result shaping (filter-adjacent contract)

| Aspect              | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Provenance                                   |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- |
| Limits              | tasks/projects default 25; folders hard-cap 100 (name-sorted); export 1000 (5000 with `outputDirectory`)                                                                                                                                                                                                                                                                                                                                                                                               | `OmniFocusReadTool`                          |
| Truncation honesty  | **Normative (Q3 resolved 2026-06-11):** `metadata.total_count` reports the FULL matching population (not the returned rows), and `metadata.truncated: true` appears whenever `returned_count < total_count`. Current behavior drifts: total_count echoes returned_count, making truncation structurally invisible (live probe: `limit:2` on a 49-task flagged population returned `total_count: 2`) — §8 D13, OMN-154. Until that ships: audits pass `limit: 500+`; flat-list absence is non-evidence. | P4; vault Project Audit 2026-05-19 pattern G |
| `countOnly`         | Tasks, projects, tags, folders (NOT perspectives/export — OMN-174); returns count metadata (`total_count`), no rows; MUST use the identical predicate/population as the row query (an inbox triple-bug violated this — commit `aa4be54`). Real fast path on tasks ("33x") and projects (`limit:0`+lite skips row projection + per-project enrichment); on tags/folders it trims the response only (those scripts already enumerate every row).                                                         | OMN docs "33x faster"; OMN-174               |
| Sort                | User sort embeds in OmniJS (sort-before-limit); mode-default sorts apply post-hoc                                                                                                                                                                                                                                                                                                                                                                                                                      | `OmniFocusReadTool`                          |
| Fields projection   | Post-hoc; `id` always included; absent ≠ null (absent = not requested; null = explicitly cleared in OF)                                                                                                                                                                                                                                                                                                                                                                                                | `task-query-pipeline`                        |
| Summary suppression | Narrow lookups (search/id filters) suppress the dashboard summary block; broad browses keep it                                                                                                                                                                                                                                                                                                                                                                                                         | OMN-19                                       |
| Performance classes | Fast: `id`, `projectId`, `parentTaskId`, `countOnly`. Full-scan: `text`/`search`/`name` (~7–10 s whole-DB floor). `fastSearch` narrows scan work, not its class.                                                                                                                                                                                                                                                                                                                                       | OMN-115; memory perf_whole_db_count_floor    |

## 6. Projects, folders, tags queries

| Query        | Filter contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Provenance                                                            |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| projects     | Compiled via `transformProjectFilters` (OMN-156). Supported keys: `status`, `completed`, `flagged`, `name`, `text`, `folder`, `id`. `status` wire values `active`/`completed`/`dropped`/`on_hold` (P1: strict schema rejects internal `onHold`/`done` vocabulary); `completed:true`→done, `completed:false`→active+onHold (dropped excluded — GTD/parity decision, see OMN-151/156 design doc §3.3); `status` and `completed` may intersect but disjoint combinations reject; `id` is an exclusive fast path (co-filters reject); `folder: "<name>"` substring, `folder: null` = top-level only; `name` name-scoped; `text` name-or-note (wired OMN-142); `matches` via safe `new RegExp(JSON.stringify(...))`; AND merges. **OR/NOT (OMN-171 S3):** `OR: [...]` compiles each branch to a flat `ProjectFilter` → `orBranches`, joined with `\|\|` and ANDed with base keys (mirror of the tasks orBranches path); `NOT: {status}` compiles to the **four-state complement** (`active`/`onHold`/`done`/`dropped` minus the named state) folded into `status` by intersection (empty intersection = contradiction → reject). Unlike the tasks NOT (completed/active only), all four `status` values are valid. Empty `OR:[]`/empty OR branch reject; OR branches enforce the same supported-key + id-exclusivity contract. Task-only keys (tags, project, projectId, parentTaskId, dueDate, deferDate, etc.) reject naming the key. | OMN-96, OMN-142, OMN-40, OMN-156, OMN-171                             |
| folders      | Name-sorted, capped 100; order/absence checks need a direct osascript probe. **Filter contract (OMN-161 S1 → S2/OMN-170):** supported keys `name` (`{contains}`/`{matches}`) and `folder` (`"<name>"` = parent folder name substring; `null` = top-level only, mirroring projects). `total_available` reports the matching population (pre-limit, OMN-154). Every other key (status, flagged, id, logical operators, task keys) rejects with a steering error naming `type:"folders"` and the supported set; `satisfies`-exhaustive `FOLDER_KEY_DISPOSITION`. S1 made it reject-all (P1 honesty); S2 added the capability.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | memory tooling_omnifocus_read_defaults; OMN-161 S1, OMN-170           |
| tags         | Basic mode only (`{id, name}`) — tag hierarchy is writable but **unreadable** through the read seam (§8 D3, OMN-145). **Filter contract (OMN-161 S1 → S2/OMN-170):** supported key `name` (`{contains}`/`{matches}`) filters tags by name; `summary.total_matched` reports the matching population (OMN-154). Every other key (incl. `folder`, logical operators) rejects with a steering error naming `type:"tags"`; `satisfies`-exhaustive `TAG_KEY_DISPOSITION`. S1 made it reject-all (P1 honesty); S2 added the capability.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | OMN-145; OMN-161 S1, OMN-170                                          |
| perspectives | **Normative (Q4 resolved 2026-06-11):** custom-perspective filter rules WILL be surfaced — OmniJS `archivedFilterRules` is reachable (live probe returned rule counts for all three custom perspectives tested; the legacy script's "JXA doesn't expose filter rules" comment aged out). Implementation OMN-155. Until it ships, `filterRules` returns null and dependent workflows (tag migrations) must manually verify perspective filters. **Filter contract (OMN-161 S1):** any `filters` key rejects with a steering error naming `type:"perspectives"` — previously accepted any key and silently ignored it (P1 violation).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | vault Tag Inventory Redesign 2026-06-03; probe 2026-06-11; OMN-161 S1 |

Project-review fields (`nextReviewDate`, `lastReviewDate`, `reviewInterval`, a `needsReview` predicate) are required by
the weekly-review workflow (§7 R8) and are projects-only; task queries requesting them get a steering error, not a bare
reject (type-discriminated field enums — vault Description-Gap Audit gap #2; OMN-130 territory).

## 7. Requirements register (GTD layer — from the vault)

| #   | Requirement                                                                                                             | Source                                | Status                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------------------------------- |
| R1  | `name` matches name only; a note-only match returning under `name` is a data-loss bug                                   | Diary 2026-06-09                      | Met (`d89d5b7`); conformance case C1                                                |
| R2  | Operators are distinct: `matches` = real regex, `contains` = substring; silent degradation is a defect                  | Diary 2026-06-11                      | Met (projects in `d89d5b7`; tasks via OMN-149 `144d7b5`)                            |
| R3  | Filters compose; unsupported combinations error loudly (never partial application)                                      | Diary 2026-06-11                      | Met (name+text `d89d5b7`; NOT hard-rejects via OMN-131 `5b41534`)                   |
| R4  | Mode views exclude all terminal states (dropped leak found during weekly review)                                        | gtd-review-handoff.md                 | Met — all modes + bare queries (D14 resolved via OMN-157, PR #90)                   |
| R5  | `plannedDate` is a first-class peer of dueDate (planned-cluster + Sunday GC workflow); "past-planned" ≠ "overdue"       | Diary 2026-05-21, 2026-04-30          | Met (filters); mode split pending OMN-133                                           |
| R6  | `overdue` reconcilable with Forecast Past via a documented query pair                                                   | Diary 2026-05-06, 2026-05-20          | Partial — recipe documented (§4); OMN-133 open                                      |
| R7  | Exclusion filters net out recurring noise (`tags:{none:["Bills"]}` + mode)                                              | Diary 2026-05-20                      | Met — needs conformance case C4                                                     |
| R8  | Review-cycle queryability (needsReview, last-reviewed age) for weekly review step 5                                     | Project Audit 2026-05-19              | Open (OMN-130 territory)                                                            |
| R9  | Availability is queryable and matches OF's model incl. sequential blocking (project-health % available)                 | Project Audit 2026-05-19              | Met — needs sequential-fixture conformance case C13                                 |
| R10 | Truncation unmistakable; default limits documented per query type                                                       | Project Audit 2026-05-19 pattern G    | Violated — total_count echoes returned rows (D13, OMN-154)                          |
| R11 | Cache never serves a different predicate (OR-of-names returned a stale generic 25-row slice, `from_cache:true`)         | Diary 2026-05-19                      | Re-diagnosed: predicate dropped pre-cache (D10, OMN-156); cache re-verify after fix |
| R12 | Aggregate→worklist parity: every analyze aggregate has a documented read query returning exactly the counted population | Diary 2026-05-18                      | Spec stance adopted; verification deferred to the analyze section of OMN-148        |
| R13 | Fixtures model real scale: ~3,000 tasks, 160+ projects, recurrence ghosts, case-duplicate tags                          | Project Audit, Tag Inventory Redesign | Conformance-suite design input                                                      |

## 8. Drift register

| ID  | Spec statement                                                                                      | Current behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Severity                                                             | Ticket                                              |
| --- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------- |
| D1  | P3: unsupported NOT errors                                                                          | ~~Silently compiled to match-ALL~~ RESOLVED 2026-06-11: hard-reject shipped                                                                                                                                                                                                                                                                                                                                                                                                                   | High (P5: feeds destructive sweeps)                                  | OMN-131 (closed, `5b41534`)                         |
| D2  | R6: Forecast-Past parity mode                                                                       | dueDate-only `overdue`; manual two-query recipe                                                                                                                                                                                                                                                                                                                                                                                                                                               | Medium                                                               | OMN-133 (open, held on OMN-130)                     |
| D3  | Tag hierarchy readable (P7 symmetry)                                                                | Tags query hardcoded basic mode                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Medium                                                               | OMN-145 (open)                                      |
| D4  | `between` sets the operator for every date field                                                    | `deferDate` entry lacks `operatorKey`; functional impact likely nil (inclusive bounds equivalent) — verify, then fix for consistency                                                                                                                                                                                                                                                                                                                                                          | Low                                                                  | batch with conformance-suite section (Q5)           |
| D5  | `upcoming` default documented = implemented                                                         | ~~Tool description says 14 while its own example and code say 7~~ RESOLVED 2026-06-12: description corrected to 7 (rider in the OMN-151/156 PR; no longer needs the OMN-154 fold-in)                                                                                                                                                                                                                                                                                                          | Low                                                                  | OMN-151/156 PR                                      |
| D6  | `name:{matches}` safe regex on tasks                                                                | ~~Task-side raw interpolation~~ RESOLVED 2026-06-11: `RegExp`-constructor escape shipped                                                                                                                                                                                                                                                                                                                                                                                                      | High                                                                 | OMN-149 (closed, `144d7b5`)                         |
| D7  | P8: tasks `folder` filter is effective                                                              | ~~Zero implementation — silently no-ops~~ RESOLVED 2026-06-12: tasks/export queries now reject `folder` with a steering error naming the projects-by-folder → tasks-by-projectId recipe (OMN-162). Capability (actual folder-scoped task results) parked as OMN-167.                                                                                                                                                                                                                          | High (P8 violation, silent no-op)                                    | OMN-162 (shipped: rejection; capability = OMN-167)  |
| D8  | `parentTaskId` integration-verified                                                                 | Builder coverage only; no live validation                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Low                                                                  | OMN-135 adjacent                                    |
| D9  | Combined-filter RESULT correctness integration-tested (text+date+tag, OR)                           | No integration test exercises OR result correctness                                                                                                                                                                                                                                                                                                                                                                                                                                           | Medium                                                               | OMN-135 (open)                                      |
| D10 | P3: projects queries never silently widen                                                           | ~~RE-DIAGNOSED 2026-06-11: projects compile path silently DROPS `OR` → match-all.~~ RESOLVED 2026-06-12: projects filters compile via typed `transformProjectFilters`; OR no longer silently dropped.                                                                                                                                                                                                                                                                                         | High                                                                 | OMN-156 (fix landed 2026-06-12; closes at PR merge) |
| D11 | Docs match shipped composition semantics                                                            | ~35 stale doc claims incl. "OR is broken"                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Low                                                                  | OMN-104 (open)                                      |
| D12 | §3.4: project root rows excluded by default, marked when opted in                                   | All tasks queries return roots unmarked, indistinguishable from real tasks (N+1; live-confirmed 2026-06-11)                                                                                                                                                                                                                                                                                                                                                                                   | High (P5: root row in a write sweep)                                 | OMN-153 (open)                                      |
| D13 | §5: `total_count` = full matching population + `truncated` flag                                     | `total_count` echoes `returned_count` (live probe: limit 2 vs population 49) — truncation structurally invisible on tasks AND projects                                                                                                                                                                                                                                                                                                                                                        | High (P4: fed the May audit near-miss)                               | OMN-154 (open)                                      |
| D14 | §3.1/§4: ALL modes + bare queries exclude completed AND dropped                                     | ~~dropped tasks leaked from search/all/inbox/bare paths~~ RESOLVED via AST-routed default (PR #90; first attempt's `if (task.dropped)` check was a runtime no-op — synthetic field). Behavioral integration test pins it.                                                                                                                                                                                                                                                                     | Medium (pollutes selections)                                         | OMN-157 (closed, PR #90)                            |
| D15 | P2/P3: logical-operator composition correct                                                         | ~~Siblings beside AND/OR/NOT were silently dropped (V1); AND merged last-wins instead of conflict-checking; AND+OR together miscompiled; `OR: []`/`AND: []` compiled to match-all~~ RESOLVED 2026-06-12: all AND-compose; conflicts, empty operators, and empty items reject with VALIDATION_ERROR.                                                                                                                                                                                           | High (P5: feeds destructive sweeps)                                  | OMN-151 (fix landed 2026-06-12; closes at PR merge) |
| D16 | §4: modes AND-compose with OR filters                                                               | ~~`buildAST` dropped base keys beside `orBranches` — modes (e.g. `mode:'flagged'`) and defaults were silently ignored when combined with OR filters, leaking unflagged/dropped tasks~~ RESOLVED 2026-06-12 (OMN-151).                                                                                                                                                                                                                                                                         | High (P5: feeds destructive sweeps)                                  | OMN-151 (fix landed 2026-06-12; closes at PR merge) |
| D17 | P1: folders/tags/perspectives queries reject UNSUPPORTED filter keys (accept-then-ignore forbidden) | ~~folders and tags queries accepted any `filters` key and ran the tasks-side transform, then silently discarded the result; perspectives likewise~~ RESOLVED OMN-161 S1 (reject-all) → OMN-170 S2 (capability): folders accept `name`/`folder`, tags accept `name`; every other key rejects with a steering error naming the query type. Perspectives stay reject-all. `satisfies`-exhaustive per-type disposition tables.                                                                    | High (P1 violation; silently inert filters on all three query types) | OMN-161 S1, OMN-170                                 |
| D18 | Rejection messages name the query type that received the filter (F6)                                | ~~folders/tags rejections reported the error as if from a `tasks or export` query — wrong type in the steering message~~ RESOLVED OMN-161 S1: rejection message names the actual query type (`folders`, `tags`, `perspectives`).                                                                                                                                                                                                                                                              | Low (misleading error text)                                          | OMN-161 S1                                          |
| D19 | `status:"on_hold"` rejection on tasks/export names its origin branch (F5)                           | ~~when `status:"on_hold"` appeared inside an OR branch the rejection message carried a bare key path without identifying the branch — confusing for nested-OR debugging~~ RESOLVED OMN-161 S1: the error path is branch-qualified (e.g. `OR[1].status`).                                                                                                                                                                                                                                      | Low (confusing error path)                                           | OMN-161 S1                                          |
| D20 | P1: projects AND-filter rejects empty items (F7)                                                    | ~~an `AND` array containing an empty-object item (`{}`) was silently accepted and compiled as a no-op, violating P1 (accept-then-ignore)~~ RESOLVED OMN-161 S1: empty AND items reject with VALIDATION_ERROR.                                                                                                                                                                                                                                                                                 | Low (silent no-op inside AND)                                        | OMN-161 S1                                          |
| D21 | Export compile path contains no dead code reads (F9)                                                | ~~the task export compile path read `compiled.filters.search` which was always `undefined` — the public API never populates it (legacy `TaskFilter` field not mapped); the read was inert but misleading dead code~~ RESOLVED OMN-161 S1: dead line removed; `ExportFilter.search` retained in the type for the script-builder side.                                                                                                                                                          | Low (dead code, no behavioral impact)                                | OMN-161 S1                                          |
| D22 | P3: an OR branch requesting a terminal state the base excludes is not silently unsatisfiable (F8)   | ~~tasks queries exclude dropped (and completed) at the base by default; an OR branch requesting `status:'dropped'`/`'completed'` compiled to `dropped==false AND (dropped==true OR …)` — the branch was silently unsatisfiable~~ RESOLVED OMN-161 S4 (OMN-172): the contradiction rejects with a VALIDATION_ERROR steering toward the top-level `status` form; covers the `completed` sibling symmetrically. `includeCompleted` is export-only, so the rule is tasks-only.                    | High (P3: silently empty branch feeds wrong selections)              | OMN-172                                             |
| D23 | `filter_description` reflects every key present in `filters_applied` (F10)                          | ~~`describeFilterForScript` described only a subset of keys (defer/planned/completion/added dates, estimatedMinutes, tagStatusValid, parentTaskId missing), so a filter on a missing-branch key read `filter_description:"all tasks"` while `filters_applied` echoed the key~~ RESOLVED OMN-161 S4 (OMN-172): branches backfilled + a `satisfies`-backed forcing-function test asserts every describable `NormalizedTaskFilter` key yields a fragment; `describeProjectFilter` `id` likewise. | Medium (self-contradictory metadata)                                 | OMN-172                                             |

## 9. Conformance golden cases (transport-level: MCP request → golden response)

Implementation-independent; each runs against a seeded fixture DB (design per R13).

| #   | Case                                                                         | Asserts                                                                                                                                                              |
| --- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | `name:{contains}` with note-only marker fixture                              | Note-only match NOT returned (R1 regression lock)                                                                                                                    |
| C2  | Same term via `name` vs `text`                                               | `text` ⊇ `name`; sets differ on the note-only fixture                                                                                                                |
| C3  | `name` + `text` together                                                     | Both apply (AND); neither dropped                                                                                                                                    |
| C4  | `mode:overdue` + `tags:{none}`                                               | Mode and exclusion compose (R7)                                                                                                                                      |
| C5  | `matches` with regex metacharacters vs `contains` of same string             | Results differ; regex semantics real (R2); include a `/`-bearing pattern (OMN-149 probe)                                                                             |
| C6  | `dueDate:{between}` boundary fixtures                                        | Inclusive both ends; null-due tasks absent                                                                                                                           |
| C7  | `estimatedMinutes:{lessThan}`                                                | No-estimate tasks absent (null ≠ 0)                                                                                                                                  |
| C8  | Unsupported `NOT:{tags}`                                                     | Validation error, zero rows — never match-all (post-OMN-131)                                                                                                         |
| C9  | Each mode against a fixture containing a dropped task                        | Dropped task absent from every mode                                                                                                                                  |
| C10 | `mode:overdue` vs `plannedDate:{before:now}` union                           | Documented Forecast-Past recipe returns the fixture's expected set (R6)                                                                                              |
| C11 | `limit:5` against a 10-row population                                        | `metadata.total_count: 10`; truncation visible (R10)                                                                                                                 |
| C12 | `project:null`, `folder:null` (projects), tasks `folder` → rejects (OMN-162) | Inbox / top-level / explicit rejection semantics                                                                                                                     |
| C13 | Sequential-project fixture                                                   | `available` excludes second-in-sequence; `blocked` includes it (R9)                                                                                                  |
| C14 | `projectId` task query on an N-task project                                  | Exactly N rows by default (no root); with `includeProjectRoot:true` N+1 and the root row carries `isProjectRoot:true` (§3.4); `countOnly` agrees under both settings |
| C15 | `countOnly:true` vs same query without                                       | count == rows length (same predicate)                                                                                                                                |
| C16 | `fastSearch:true` with note-only marker                                      | Note-only match absent                                                                                                                                               |
| C17 | Repeat C-series query after an unrelated cached browse                       | Response matches predicate, not cache (R11) — re-verify after OMN-156, which removes the predicate-drop that masked this                                             |
| C18 | `OR` of two name filters on projects                                         | Steering validation error, never match-all — SHIPPED (OMN-156, 2026-06-12); integration-pinned in `tests/integration/projects-filter-rejection.test.ts`.             |

## 10. Open questions (review gate)

| #   | Question                                          | Options                                                                                                                                                                                |
| --- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q1  | ~~Project root node in `projectId` task queries~~ | **RESOLVED 2026-06-11:** exclude by default + `includeProjectRoot:true` knob + `isProjectRoot:true` marker (§3.4, D12, OMN-153)                                                        |
| Q2  | ~~`upcoming` default window~~                     | **RESOLVED 2026-06-11:** 7 stays; description's "14" was a self-contradiction, fixed via OMN-154 (§4, D5)                                                                              |
| Q3  | ~~Truncation indicator~~                          | **RESOLVED 2026-06-11:** probe showed `total_count` echoes returned rows (2 vs population 49) — normative: population + `truncated` flag (§5, D13, OMN-154)                            |
| Q4  | ~~Perspectives `filterRules: null`~~              | **RESOLVED 2026-06-11:** commit to surfacing — OmniJS `archivedFilterRules` live-proven reachable (§6, OMN-155)                                                                        |
| Q5  | ~~D4/D5/D7/D10 tickets~~                          | **RESOLVED 2026-06-11:** D10 re-diagnosed (projects OR silently dropped, not cache) and filed High (OMN-156); D5 folded into OMN-154; D4 + D7 batch with the conformance-suite section |

All five §10 questions are resolved; the section is retained as the decision record.
