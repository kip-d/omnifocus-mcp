# Archived: DOT process workflows

Retired 2026-07-14. A transcript audit (65 sessions, prior 7 days) found `CLAUDE-PROCESSES.dot` was never
read in-session — every mention was the CLAUDE.md context injection, zero tool calls touched the file. The
companion `DOT-EFFECTIVENESS-TRACKING.md` manual experiment never ran; the transcript audit answered its
question retroactively: prose wins.

Load-bearing content was extracted to CLAUDE.md ("Process Rules" + the JXA-vs-Bridge decision line). TDD,
systematic debugging, and pre-completion verification were already delivered by superpowers skills and are
not restated anywhere.

The archived DOT is a frozen snapshot of its final pre-retirement state on main. Its internal header comment
still names the original `.claude/processes/` path in the PNG-regeneration command — deliberately left as-is;
regenerate from this directory instead if ever needed.
